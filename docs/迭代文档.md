EduSocratic MVP 分级开发文档

> **当前进度（2026-07-24）**：✅ Level 0 项目骨架 · ✅ Level 1 领域模型 · ✅ Level 2 Prompt+LLM 管道 · ✅ Level 3 飞书 Webhook。下一个：Level 4（飞书卡片 + 评估串联）。
> 实现备注：22 个测试全绿；与本文档的代码偏差（uv 依赖组配置、openai 空 key 守卫、markdown 损坏还原清单）见 CLAUDE.md「迭代模式」一节。

给 Claude 的对接文档
目标：从零构建 MVP 核心链路
核心链路：老师 @机器人 输入文字 → AI 六维评估 → 飞书卡片返回 → 老师确认/打回

全局约定

技术栈
组件   选型   版本
语言   Python   3.11+

Web 框架   FastAPI   0.100+

LLM 调用   openai SDK   1.0+

数据校验   Pydantic   2.0+

数据库   PostgreSQL + SQLAlchemy   2.0+

缓存   Redis + redis-py   5.0+

飞书 SDK   lark-oapi   1.0+

流程编排   LangGraph   0.2+

包管理   uv   latest

测试   pytest + pytest-asyncio   latest

目录结构（最终形态）

edusocratic/
├── pyproject.toml
├── .env.example
├── alembic/                    # 数据库迁移
│   └── versions/
├── app/
│   ├── init.py
│   ├── main.py                 # FastAPI 入口
│   ├── config.py               # 配置管理
│   ├── domain/                 # 领域模型（纯数据，无 IO）
│   │   ├── init.py
│   │   ├── schemas.py          # Pydantic 模型
│   │   ├── grade_weights.py    # 年级权重
│   │   └── constants.py        # 常量
│   ├── prompts/                # Prompt 模板
│   │   ├── init.py
│   │   ├── assessment_1_2.py
│   │   ├── assessment_3_4.py
│   │   ├── assessment_5_7.py
│   │   └── repair.py
│   ├── graphs/                 # LangGraph 流程
│   │   ├── init.py
│   │   └── assessment_graph.py
│   ├── services/               # 业务逻辑
│   │   ├── init.py
│   │   ├── llm_service.py      # LLM 调用封装
│   │   ├── assessment_service.py
│   │   └── feishu_service.py   # 飞书 API 封装
│   ├── api/                    # HTTP 端点
│   │   ├── init.py
│   │   ├── webhook.py          # 飞书事件回调
│   │   └── callback.py         # 卡片按钮回调
│   ├── models/                 # SQLAlchemy 模型
│   │   ├── init.py
│   │   └── tables.py
│   └── utils/
│       ├── init.py
│       └── text_parser.py      # 解析老师输入
├── tests/
│   ├── conftest.py
│   ├── test_domain.py
│   ├── test_prompts.py
│   ├── test_graph.py
│   ├── test_llm_service.py
│   ├── test_webhook.py
│   ├── test_callback.py
│   └── test_integration.py
└── scripts/
    └── seed_data.py

环境变量（.env.example）

LLM
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini
LLM_TIMEOUT=30
LLM_MAX_RETRIES=2

飞书
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx
FEISHU_VERIFICATION_TOKEN=xxx
FEISHU_ENCRYPT_KEY=xxx

数据库
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/edusocratic

Redis
REDIS_URL=redis://localhost:6379/0

配额
DAILY_LIMIT_PER_STUDENT=20
MONTHLY_BUDGET_CENTS=5000
CACHE_TTL_SECONDS=86400

Level 0：项目骨架

目标

创建可运行的 FastAPI 空项目，uv run uvicorn app.main:app 能启动，/health 返回 200。

前置依赖

无。

要实现的文件

pyproject.toml

[project]
name = "edusocratic"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "openai>=1.0.0",
    "langgraph>=0.2.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.28.0",
    "alembic>=1.12.0",
    "redis>=5.0.0",
    "lark-oapi>=1.0.0",
    "httpx>=0.24.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100

app/config.py

from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # LLM
    openai_api_key: str = ""
    openai_model: str = "gpt-4o-mini"
    llm_timeout: int = 30
    llm_max_retries: int = 2

    # 飞书
    feishu_app_id: str = ""
    feishu_app_secret: str = ""
    feishu_verification_token: str = ""
    feishu_encrypt_key: str = ""

    # 数据库
    database_url: str = "postgresql+asyncpg://user:pass@localhost:5432/edusocratic"

    # Redis
    redis_url: str = "redis://localhost:6379/0"

    # 配额
    daily_limit_per_student: int = 20
    monthly_budget_cents: int = 5000
    cache_ttl_seconds: int = 86400

    class Config:
        env_file = ".env"

settings = Settings()

app/main.py

from fastapi import FastAPI

app = FastAPI(title="EduSocratic", version="0.1.0")

@app.get("/health")
async def health():
    return {"status": "ok", "version": "0.1.0"}

验收标准

安装依赖
uv sync

启动服务
uv run uvicorn app.main:app --reload

测试健康检查
curl http://localhost:8000/health
期望：{"status":"ok","version":"0.1.0"}

运行测试（此时无测试文件，pytest 应通过）
uv run pytest

完成标志

[ ] uv sync 无报错
[ ] curl /health 返回 200
[ ] 目录结构已创建（空 init.py 文件）

Level 1：领域模型

目标

定义所有 Pydantic 数据模型、年级权重、评估维度常量。纯数据层，无 IO，无外部依赖。

前置依赖

Level 0 完成。

要实现的文件

app/domain/constants.py

"""评估维度与常量定义"""

六个评估维度（固定顺序）
DIMENSIONS = [
    "观点清晰度",
    "逻辑连贯性",
    "证据支撑",
    "多角度思考",
    "回应与质疑",
    "表达完整性",
] as const  # 实际用 tuple

DIMENSIONS: tuple[str, ...] = (
    "观点清晰度",
    "逻辑连贯性",
    "证据支撑",
    "多角度思考",
    "回应与质疑",
    "表达完整性",
)

分数范围
SCORE_MIN = 1
SCORE_MAX = 5

年级范围
GRADE_MIN = 1
GRADE_MAX = 7

文本长度限制
TEXT_MIN_LENGTH = 10
TEXT_MAX_LENGTH = 2000

打回次数上限
MAX_RETRY_COUNT = 2

评估状态
class AssessmentStatus:
    PENDING = "pending"
    CONFIRMED = "confirmed"
    MODIFIED = "modified"
    RETRYING = "retrying"
    DISPUTED = "disputed"
    RESOLVED = "resolved"

来源类型
class SourceType:
    HANDWRITING = "handwriting"
    VOICE = "voice"
    TEXT = "text"

app/domain/grade_weights.py

"""年级差异化权重"""

from app.domain.constants import DIMENSIONS

每个年级段的权重（顺序与 DIMENSIONS 一致）
GRADE_WEIGHTS: dict[str, dict[str, float]] = {
    "1_2": {
        "观点清晰度": 0.30,
        "逻辑连贯性": 0.00,  # 不评估
        "证据支撑": 0.25,
        "多角度思考": 0.00,  # 不评估
        "回应与质疑": 0.20,
        "表达完整性": 0.25,
    },
    "3_4": {
        "观点清晰度": 0.25,
        "逻辑连贯性": 0.15,
        "证据支撑": 0.20,
        "多角度思考": 0.15,
        "回应与质疑": 0.15,
        "表达完整性": 0.10,
    },
    "5_7": {
        "观点清晰度": 0.20,
        "逻辑连贯性": 0.20,
        "证据支撑": 0.20,
        "多角度思考": 0.20,
        "回应与质疑": 0.10,
        "表达完整性": 0.10,
    },
}

def get_grade_band(grade: int) -> str:
    """年级 → 年级段"""
    if grade in (1, 2):
        return "1_2"
    elif grade in (3, 4):
        return "3_4"
    else:  # 5, 6, 7
        return "5_7"

def get_weights(grade: int) -> dict[str, float]:
    """获取指定年级的权重"""
    band = get_grade_band(grade)
    return GRADE_WEIGHTS[band]

def get_active_dimensions(grade: int) -> list[str]:
    """获取该年级实际评估的维度（权重>0的）"""
    weights = get_weights(grade)
    return [dim for dim in DIMENSIONS if weights[dim] > 0]

def compute_weighted_score(scores: dict[str, int], grade: int) -> float:
    """计算加权总分"""
    weights = get_weights(grade)
    total = sum(scores[dim] * weights[dim] for dim in DIMENSIONS)
    return round(total, 2)

app/domain/schemas.py

"""Pydantic 数据模型"""

from pydantic import BaseModel, Field, field_validator
from app.domain.constants import DIMENSIONS, SCORE_MIN, SCORE_MAX, GRADE_MIN, GRADE_MAX

─── 评估输入 ───

class AssessmentInput(BaseModel):
    """评估请求输入"""
    text: str = Field(..., min_length=1, description="学生发言/作文文字")
    grade: int = Field(..., ge=GRADE_MIN, le=GRADE_MAX)
    topic: str = Field(..., min_length=1, max_length=100)
    student_id: str = Field(..., min_length=1)
    class_id: str = Field(..., min_length=1)
    source_type: str = Field(default="text")  # handwriting / voice / text
    teacher_context: str | None = None  # 打回时老师补充的上下文
    retry_count: int = Field(default=0, ge=0, le=2)

─── LLM 原始输出 ───

class LLMAssessmentOutput(BaseModel):
    """LLM 返回的原始评估结果（用于 Pydantic 校验）"""
    观点清晰度: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    逻辑连贯性: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    证据支撑: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    多角度思考: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    回应与质疑: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    表达完整性: int = Field(..., ge=SCORE_MIN, le=SCORE_MAX)
    reasoning: str = Field(..., min_length=1)
    highlights: str = Field(..., min_length=1)
    suggestions: str = Field(..., min_length=1)
    overall_comment: str = Field(..., min_length=1)

    @field_validator("观点清晰度", "逻辑连贯性", "证据支撑", "多角度思考", "回应与质疑", "表达完整性")
    @classmethod
    def score_must_be_int(cls, v):
        if not isinstance(v, int):
            raise ValueError(f"分数必须是整数，收到 {type(v)}")
        return v

─── 评估结果（完整） ───

class AssessmentResult(BaseModel):
    """完整评估结果（含加权分）"""
    scores: dict[str, int]  # 六维分数
    weighted_score: float = Field(..., ge=1.0, le=5.0)
    reasoning: str
    highlights: str
    suggestions: str
    overall_comment: str
    needs_review: bool = False
    grade: int
    topic: str
    student_id: str
    class_id: str
    source_type: str = "text"
    retry_count: int = 0
    teacher_context: str | None = None

─── 兜底结果 ───

class FallbackResult(BaseModel):
    """兜底结果（LLM 失败时）"""
    scores: dict[str, int]  # 全 3 分
    weighted_score: float
    reasoning: str = "评估暂时不可用，已标记待人工复核。"
    highlights: str = "待人工复核后补充。"
    suggestions: str = "待人工复核后补充。"
    overall_comment: str = "系统暂时无法完成评估，请稍后重试或等待人工处理。"
    needs_review: bool = True

─── 教师反馈 ───

class TeacherFeedback(BaseModel):
    """教师反馈"""
    assessment_id: int
    teacher_id: str
    action: str  # confirm / modify / disagree / retry
    modified_scores: dict[str, int] | None = None
    teacher_context: str | None = None
    comment: str | None = None

─── 飞书消息解析 ───

class ParsedCommand(BaseModel):
    """解析后的老师指令"""
    action: str  # assess / query / help / weekly_report / cancel
    student_name: str | None = None
    grade: int | None = None
    topic: str | None = None
    text: str | None = None  # 学生发言文字
    raw_message: str = ""

验收标准

tests/test_domain.py

def test_grade_weights_sum_to_one():
    """每个年级段的权重之和必须为 1.0"""
    from app.domain.grade_weights import GRADE_WEIGHTS
    for band, weights in GRADE_WEIGHTS.items():
        total = sum(weights.values())
        assert abs(total - 1.0) < 0.001, f"{band} 权重之和为 {total}"

def test_grade_1_2_excludes_logic_and_perspective():
    """1-2年级不评估逻辑连贯性和多角度思考"""
    from app.domain.grade_weights import get_weights
    w = get_weights(1)
    assert w["逻辑连贯性"] == 0.0
    assert w["多角度思考"] == 0.0

def test_compute_weighted_score():
    """加权总分计算正确"""
    from app.domain.grade_weights import compute_weighted_score
    scores = {"观点清晰度": 4, "逻辑连贯性": 3, "证据支撑": 4,
              "多角度思考": 3, "回应与质疑": 3, "表达完整性": 4}
    # 3-4年级权重
    result = compute_weighted_score(scores, grade=3)
    expected = 0.25 + 30.15 + 0.20 + 30.15 + 0.15 + 40.10
    assert abs(result - round(expected, 2)) < 0.01

def test_llm_output_validation_rejects_out_of_range():
    """分数越界应报错"""
    import pytest
    from app.domain.schemas import LLMAssessmentOutput
    with pytest.raises(Exception):
        LLMAssessmentOutput(
            观点清晰度=6,  # 越界
            逻辑连贯性=3, 证据支撑=3, 多角度思考=3,
            回应与质疑=3, 表达完整性=3,
            reasoning="test", highlights="test",
            suggestions="test", overall_comment="test"
        )

def test_llm_output_validation_rejects_float():
    """分数为小数应报错"""
    import pytest
    from app.domain.schemas import LLMAssessmentOutput
    with pytest.raises(Exception):
        LLMAssessmentOutput(
            观点清晰度=3.5,  # 小数
            逻辑连贯性=3, 证据支撑=3, 多角度思考=3,
            回应与质疑=3, 表达完整性=3,
            reasoning="test", highlights="test",
            suggestions="test", overall_comment="test"
        )

def test_parsed_command_defaults():
    """解析指令默认值"""
    from app.domain.schemas import ParsedCommand
    cmd = ParsedCommand(action="assess", raw_message="test")
    assert cmd.student_name is None
    assert cmd.grade is None

完成标志

[ ] uv run pytest tests/test_domain.py 全部通过
[ ] 所有 Pydantic 模型可正常实例化
[ ] 权重之和校验通过

Level 2：Prompt 模板 + LLM 调用

目标

实现 Prompt 渲染、LLM 调用、三段式校验、修复重试、兜底。这是评估管道的核心。

前置依赖

Level 1 完成。

要实现的文件

app/prompts/assessment_3_4.py（示例，1_2 和 5_7 类似）

"""3-4年级评估 Prompt"""

SYSTEM_PROMPT = """你是一位专业的少儿思辨课评估专家。你需要评估一名{grade}年级学生在思辨课上的表现。

评估维度与评分标准（3-4年级）

观点清晰度（权重25%）
1分：没有明确观点，或完全跑题
2分：有模糊的观点，但表述不清
3分：有明确观点，能用一句话说清楚
4分：观点明确，表述清晰，有展开
5分：观点鲜明，表述精准，有层次感

逻辑连贯性（权重15%）
1分：前后矛盾，完全没有逻辑
2分：有简单逻辑，但跳跃明显
3分：有基本的因果链（因为...所以...）
4分：因果链完整，有层次
5分：逻辑严密，能分层论证

证据支撑（权重20%）
1分：纯主观，没有任何支撑
2分：有模糊的支撑（"好像是这样"）
3分：有一个具体例子或理由
4分：有具体例子+解释
5分：多个证据，且能解释为什么这些证据支持观点

多角度思考（权重15%）
1分：只有一面，完全没考虑其他可能
2分：隐约提到另一面，但没展开
3分：能提到"但是..."或"反方可能觉得..."
4分：能展开反方观点
5分：能对比正反方，并给出自己的判断

回应与质疑（权重15%）
1分：自说自话，完全没回应他人
2分：提到他人观点但没回应
3分：能简单回应（"我不同意，因为..."）
4分：能针对性反驳
5分：能反驳+追问+提出新角度

表达完整性（权重10%）
1分：词不达意，残缺不全
2分：基本能懂，但有明显缺失
3分：表达基本完整通顺
4分：表达流畅，结构清晰
5分：表达优美，有感染力

重要规则
你评估的是思维质量，不是语法。错别字、病句不扣分。
口语填充词（"嗯""那个""就是"）不扣分。
分数必须是 1-5 的整数。
必须严格按照 JSON 格式输出。
"""

USER_PROMPT = """## 讨论主题
{topic}

学生发言/作文
{text}

{teacher_context_section}

输出要求
请严格按以下 JSON 格式输出，不要输出任何其他内容：

json
{{
  "观点清晰度": <1-5整数>,
  "逻辑连贯性": <1-5整数>,
  "证据支撑": <1-5整数>,
  "多角度思考": <1-5整数>,
  "回应与质疑": <1-5整数>,
  "表达完整性": <1-5整数>,
  "reasoning": "<评分理由，2-3句话>",
  "highlights": "<亮点，1-2句话，给家长看的，语气鼓励>",
  "suggestions": "<建议，1-2句话，给家长看的，语气温和>",
  "overall_comment": "<总评，1-2句话>"
}}
"""

TEACHER_CONTEXT_TEMPLATE = """## 教师补充上下文
{context}
请结合以上上下文重新评估。"""

app/prompts/repair.py

"""修复重试 Prompt"""

REPAIR_PROMPT = """你上次的输出格式有误，无法解析。

错误信息：{error_message}

你的原始输出（前500字）：
{raw_output}

请严格按照以下 JSON 格式重新输出，不要输出任何其他内容（不要 markdown 代码块标记）：

{{
  "观点清晰度": <1-5整数>,
  "逻辑连贯性": <1-5整数>,
  "证据支撑": <1-5整数>,
  "多角度思考": <1-5整数>,
  "回应与质疑": <1-5整数>,
  "表达完整性": <1-5整数>,
  "reasoning": "<评分理由>",
  "highlights": "<亮点>",
  "suggestions": "<建议>",
  "overall_comment": "<总评>"
}}"""

app/prompts/init.py

"""Prompt 路由"""

from app.domain.grade_weights import get_grade_band

def get_assessment_prompts(grade: int) -> tuple[str, str]:
    """根据年级返回 (system_prompt, user_prompt_template)"""
    band = get_grade_band(grade)
    if band == "1_2":
        from app.prompts.assessment_1_2 import SYSTEM_PROMPT, USER_PROMPT
    elif band == "3_4":
        from app.prompts.assessment_3_4 import SYSTEM_PROMPT, USER_PROMPT
    else:
        from app.prompts.assessment_5_7 import SYSTEM_PROMPT, USER_PROMPT
    return SYSTEM_PROMPT, USER_PROMPT

def render_assessment_prompt(
    grade: int,
    topic: str,
    text: str,
    teacher_context: str | None = None,
) -> list[dict[str, str]]:
    """渲染完整的 messages 列表"""
    system_tpl, user_tpl = get_assessment_prompts(grade)

    system = system_tpl.format(grade=grade)

    # 教师上下文部分
    if teacher_context:
        from app.prompts.assessment_3_4 import TEACHER_CONTEXT_TEMPLATE
        ctx_section = TEACHER_CONTEXT_TEMPLATE.format(context=teacher_context)
    else:
        ctx_section = ""

    user = user_tpl.format(
        topic=topic,
        text=text,
        teacher_context_section=ctx_section,
    )

    return [
        {"role": "system", "content": system},
        {"role": "user", "content": user},
    ]

app/services/llm_service.py

"""LLM 调用封装：调用 + 校验 + 修复重试 + 兜底"""

import json
import logging
from openai import AsyncOpenAI
from pydantic import ValidationError

from app.config import settings
from app.domain.schemas import LLMAssessmentOutput, AssessmentResult, FallbackResult
from app.domain.grade_weights import compute_weighted_score, get_active_dimensions
from app.prompts import render_assessment_prompt
from app.prompts.repair import REPAIR_PROMPT

logger = logging.getLogger(name)

client = AsyncOpenAI(api_key=settings.openai_api_key)

async def call_llm_assessment(
    text: str,
    grade: int,
    topic: str,
    student_id: str,
    class_id: str,
    source_type: str = "text",
    teacher_context: str | None = None,
    retry_count: int = 0,
) -> AssessmentResult:
    """
    完整的 LLM 评估调用。
    包含：调用 → 校验 → 修复重试 → 兜底。
    """
    messages = render_assessment_prompt(
        grade=grade, topic=topic, text=text, teacher_context=teacher_context
    )

    raw_output = ""
    last_error = ""

    for attempt in range(settings.llm_max_retries + 1):
        try:
            # 调用 LLM
            if attempt == 0:
                # 首次调用
                response = await client.chat.completions.create(
                    model=settings.openai_model,
                    messages=messages,
                    temperature=0,
                    max_tokens=2000,
                    timeout=settings.llm_timeout,
                )
            else:
                # 修复重试
                repair_msg = REPAIR_PROMPT.format(
                    error_message=last_error,
                    raw_output=raw_output[:500],
                )
                response = await client.chat.completions.create(
                    model=settings.openai_model,
                    messages=messages + [
                        {"role": "assistant", "content": raw_output},
                        {"role": "user", "content": repair_msg},
                    ],
                    temperature=0,
                    max_tokens=2000,
                    timeout=settings.llm_timeout,
                )

            raw_output = response.choices[0].message.content or ""

            # 三段式校验
            parsed = _validate_output(raw_output)

            # 构建结果
            scores = {
                "观点清晰度": parsed.观点清晰度,
                "逻辑连贯性": parsed.逻辑连贯性,
                "证据支撑": parsed.证据支撑,
                "多角度思考": parsed.多角度思考,
                "回应与质疑": parsed.回应与质疑,
                "表达完整性": parsed.表达完整性,
            }

            weighted = compute_weighted_score(scores, grade)

            return AssessmentResult(
                scores=scores,
                weighted_score=weighted,
                reasoning=parsed.reasoning,
                highlights=parsed.highlights,
                suggestions=parsed.suggestions,
                overall_comment=parsed.overall_comment,
                needs_review=False,
                grade=grade,
                topic=topic,
                student_id=student_id,
                class_id=class_id,
                source_type=source_type,
                retry_count=retry_count,
                teacher_context=teacher_context,
            )

        except ValidationError as e:
            last_error = str(e)
            logger.warning(f"校验失败 (attempt {attempt}): {last_error}")
            continue

        except json.JSONDecodeError as e:
            last_error = f"JSON 解析失败: {e}"
            logger.warning(f"JSON 解析失败 (attempt {attempt}): {last_error}")
            continue

        except Exception as e:
            last_error = str(e)
            logger.error(f"LLM 调用异常 (attempt {attempt}): {last_error}")
            break  # 非校验错误，直接兜底

    # 所有重试失败 → 兜底
    return _build_fallback(grade, topic, student_id, class_id, source_type, retry_count)

def _validate_output(raw: str) -> LLMAssessmentOutput:
    """三段式校验"""
    # 第一段：提取 JSON
    cleaned = raw.strip()
    # 去除可能的 markdown 代码块标记
    if cleaned.startswith(""):
        lines = cleaned.split("n")
        lines = [l for l in lines if not l.strip().startswith("")]
        cleaned = "n".join(lines)

    data = json.loads(cleaned)  # 可能抛 JSONDecodeError

    # 第二段 + 第三段：Pydantic 校验（结构 + 值域）
    return LLMAssessmentOutput(**data)  # 可能抛 ValidationError

def _build_fallback(
    grade: int, topic: str, student_id: str, class_id: str,
    source_type: str, retry_count: int,
) -> AssessmentResult:
    """构建兜底结果"""
    scores = {dim: 3 for dim in [
        "观点清晰度", "逻辑连贯性", "证据支撑",
        "多角度思考", "回应与质疑", "表达完整性",
    ]}
    weighted = compute_weighted_score(scores, grade)

    return AssessmentResult(
        scores=scores,
        weighted_score=weighted,
        reasoning="评估暂时不可用，已标记待人工复核。",
        highlights="待人工复核后补充。",
        suggestions="待人工复核后补充。",
        overall_comment="系统暂时无法完成评估，请稍后重试或等待人工处理。",
        needs_review=True,
        grade=grade,
        topic=topic,
        student_id=student_id,
        class_id=class_id,
        source_type=source_type,
        retry_count=retry_count,
    )

验收标准

tests/test_llm_service.py

import pytest
from unittest.mock import AsyncMock, patch

@pytest.mark.asyncio
async def test_call_llm_assessment_success():
    """正常调用返回完整结果"""
    mock_response = AsyncMock()
    mock_response.choices = [AsyncMock()]
    mock_response.choices[0].message.content = json.dumps({
        "观点清晰度": 4, "逻辑连贯性": 3, "证据支撑": 4,
        "多角度思考": 3, "回应与质疑": 3, "表达完整性": 4,
        "reasoning": "观点明确，有因果论证",
        "highlights": "能用因为所以来论证",
        "suggestions": "可以展开反方观点",
        "overall_comment": "达到三年级应有水平"
    })

    with patch("app.services.llm_service.client") as mock_client:
        mock_client.chat.completions.create = AsyncMock(return_value=mock_response)
        from app.services.llm_service import call_llm_assessment
        result = await call_llm_assessment(
            text="我觉得应该给动物穿衣服，因为动物会冷。",
            grade=3, topic="动物穿衣服",
            student_id="s1", class_id="c1",
        )
        assert result.weighted_score > 0
        assert result.needs_review is False
        assert result.scores["观点清晰度"] == 4

@pytest.mark.asyncio
async def test_call_llm_assessment_fallback_on_timeout():
    """LLM 超时 → 兜底"""
    with patch("app.services.llm_service.client") as mock_client:
        mock_client.chat.completions.create = AsyncMock(side_effect=Exception("timeout"))
        from app.services.llm_service import call_llm_assessment
        result = await call_llm_assessment(
            text="test", grade=3, topic="test",
            student_id="s1", class_id="c1",
        )
        assert result.needs_review is True
        assert all(v == 3 for v in result.scores.values())

def test_validate_output_rejects_markdown_wrapped():
    """能处理 markdown 代码块包裹的 JSON"""
    from app.services.llm_service import _validate_output
    raw = 'jsonn{"观点清晰度":4,"逻辑连贯性":3,"证据支撑":4,"多角度思考":3,"回应与质疑":3,"表达完整性":4,"reasoning":"t","highlights":"t","suggestions":"t","overall_comment":"t"}n'
    result = _validate_output(raw)
    assert result.观点清晰度 == 4

完成标志

[ ] uv run pytest tests/test_llm_service.py 全部通过
[ ] 正常调用返回 AssessmentResult
[ ] 超时/报错返回兜底结果（needs_review=True）
[ ] 修复重试逻辑可触发（mock 第一次返回垃圾，第二次返回正确）

Level 3：飞书 Webhook 接收 + 消息解析

目标

实现 FastAPI 端点接收飞书消息事件，解析老师输入，提取元信息。

前置依赖

Level 1 完成。

要实现的文件

app/utils/text_parser.py

"""解析老师输入的文字指令"""

import re
from app.domain.schemas import ParsedCommand

def parse_teacher_message(text: str) -> ParsedCommand:
    """
    解析老师 @机器人 后的文字。

    支持格式：
    "评估 学生:小明 年级:3 主题:动物穿衣服 发言:我觉得..."
    "小明 3年级 动物穿衣服n我觉得..."
    "帮助"
    "查询 小明"
    "生成周报"
    """
    text = text.strip()

    # 去除 @机器人 的部分（飞书会带 @mention）
    text = re.sub(r'@[w]+s*', '', text).strip()

    # 帮助
    if text in ("帮助", "help", "?"):
        return ParsedCommand(action="help", raw_message=text)

    # 查询
    if text.startswith("查询"):
        name = text.replace("查询", "").strip()
        return ParsedCommand(action="query", student_name=name or None, raw_message=text)

    # 周报
    if "周报" in text:
        return ParsedCommand(action="weekly_report", raw_message=text)

    # 取消
    if text in ("取消", "cancel"):
        return ParsedCommand(action="cancel", raw_message=text)

    # 评估（默认）
    return _parse_assessment(text)

def _parse_assessment(text: str) -> ParsedCommand:
    """解析评估请求"""
    student_name = None
    grade = None
    topic = None
    speech_text = None

    # 尝试提取 "学生:XXX" 或 "学生：XXX"
    m = re.search(r'学生[:：]s*(S+)', text)
    if m:
        student_name = m.group(1)

    # 尝试提取 "年级:N" 或 "N年级"
    m = re.search(r'年级[:：]?s*(d)', text)
    if m:
        grade = int(m.group(1))
    else:
        m = re.search(r'(d)s*年级', text)
        if m:
            grade = int(m.group(1))

    # 尝试提取 "主题:XXX" 或 "主题：XXX"
    m = re.search(r'主题[:：]s*(.+?)(?:s+(?:学生|年级|发言)|$)', text)
    if m:
        topic = m.group(1).strip()

    # 尝试提取 "发言:XXX" 或引号内的文字
    m = re.search(r'发言[:：]s*(.+)', text, re.DOTALL)
    if m:
        speech_text = m.group(1).strip()
    else:
        # 尝试引号
        m = re.search(r'""「[""」]', text, re.DOTALL)
        if m:
            speech_text = m.group(1).strip()

    # 如果以上都没提取到发言文字，尝试取最后一行非元信息文字
    if not speech_text:
        lines = text.split('n')
        # 过滤掉包含元信息关键词的行
        content_lines = [
            l for l in lines
            if not re.search(r'(学生|年级|主题|评估|发言)[:：]', l)
            and not re.search(r'^\ds年级', l)
            and l.strip()
        ]
        if content_lines:
            speech_text = 'n'.join(content_lines).strip()

    return ParsedCommand(
        action="assess",
        student_name=student_name,
        grade=grade,
        topic=topic,
        text=speech_text,
        raw_message=text,
    )

app/api/webhook.py

"""飞书事件回调端点"""

import hashlib
import logging
from fastapi import APIRouter, Request, HTTPException
from app.config import settings
from app.utils.text_parser import parse_teacher_message

logger = logging.getLogger(name)
router = APIRouter()

@router.post("/webhook")
async def feishu_webhook(request: Request):
    """接收飞书事件推送"""
    body = await request.json()

    # 1. URL 验证（飞书首次配置时发送）
    if "challenge" in body:
        return {"challenge": body["challenge"]}

    # 2. 验证 token
    token = body.get("header", {}).get("token", "")
    if token != settings.feishu_verification_token:
        raise HTTPException(status_code=403, detail="Invalid token")

    # 3. 提取事件
    event = body.get("event", {})
    event_type = body.get("header", {}).get("event_type", "")

    if event_type != "im.message.receive_v1":
        return {"code": 0}  # 忽略非消息事件

    # 4. 提取消息信息
    message = event.get("message", {})
    msg_type = message.get("message_type", "")
    chat_id = message.get("chat_id", "")
    message_id = message.get("message_id", "")
    content_str = message.get("content", "{}")
    sender = event.get("sender", {}).get("sender_id", {}).get("open_id", "")

    # 5. 检查是否 @了机器人
    mentions = message.get("mentions", [])
    is_mentioned = any(
        m.get("key") == "@_all" or m.get("id", {}).get("open_id") == settings.feishu_app_id
        for m in mentions
    ) if mentions else False

    # 如果没有 mentions 字段，检查 content 中是否有 @
    import json
    content = json.loads(content_str) if isinstance(content_str, str) else content_str

    if msg_type == "text":
        text_content = content.get("text", "")
        # 简单判断：如果消息以 @ 开头或包含 @机器人
        if not is_mentioned and "@" not in text_content:
            return {"code": 0}  # 不是 @机器人的消息，忽略

        # 解析指令
        parsed = parse_teacher_message(text_content)
        logger.info(f"解析结果: {parsed}")

        # TODO: Level 4 实现实际评估调用
        # 目前先返回解析结果（调试用）
        return {
            "code": 0,
            "data": {
                "parsed": parsed.model_dump(),
                "chat_id": chat_id,
                "message_id": message_id,
                "sender": sender,
            }
        }

    elif msg_type == "image":
        # TODO: Phase 2 实现 OCR
        logger.info(f"收到图片消息: {message_id}")
        return {"code": 0, "data": {"msg": "图片功能开发中"}}

    elif msg_type == "audio":
        # TODO: Phase 2 实现 ASR
        logger.info(f"收到语音消息: {message_id}")
        return {"code": 0, "data": {"msg": "语音功能开发中"}}

    return {"code": 0}

app/main.py（更新）

from fastapi import FastAPI
from app.api.webhook import router as webhook_router

app = FastAPI(title="EduSocratic", version="0.1.0")
app.include_router(webhook_router)

@app.get("/health")
async def health():
    return {"status": "ok", "version": "0.1.0"}

验收标准

tests/test_webhook.py

import pytest
from httpx import AsyncClient, ASGITransport
from app.main import app

@pytest.mark.asyncio
async def test_health():
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.get("/health")
        assert resp.status_code == 200

@pytest.mark.asyncio
async def test_challenge_verification():
    """飞书 URL 验证"""
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.post("/webhook", json={"challenge": "test123"})
        assert resp.json()["challenge"] == "test123"

@pytest.mark.asyncio
async def test_parse_assessment_command():
    """解析评估指令"""
    from app.utils.text_parser import parse_teacher_message
    cmd = parse_teacher_message("评估 学生:小明 年级:3 主题:动物穿衣服 发言:我觉得应该给动物穿衣服")
    assert cmd.action == "assess"
    assert cmd.student_name == "小明"
    assert cmd.grade == 3
    assert cmd.topic == "动物穿衣服"
    assert "应该给动物穿衣服" in cmd.text

@pytest.mark.asyncio
async def test_parse_help():
    from app.utils.text_parser import parse_teacher_message
    cmd = parse_teacher_message("帮助")
    assert cmd.action == "help"

@pytest.mark.asyncio
async def test_parse_query():
    from app.utils.text_parser import parse_teacher_message
    cmd = parse_teacher_message("查询 小明")
    assert cmd.action == "query"
    assert cmd.student_name == "小明"

完成标志

[ ] uv run pytest tests/test_webhook.py 全部通过
[ ] /webhook 能正确响应飞书 challenge 验证
[ ] 文字消息能被正确解析为 ParsedCommand
[ ] 非 @消息被忽略

Level 4：飞书卡片输出 + 评估调用串联

目标

收到评估请求后，调用 LLM 评估，将结果以飞书交互卡片形式发回群里。

前置依赖

Level 2 + Level 3 完成。

要实现的文件

app/services/feishu_service.py

"""飞书 API 封装：发送消息、发送卡片"""

import httpx
import logging
from app.config import settings

logger = logging.getLogger(name)

FEISHU_BASE = "https://open.feishu.cn/open-apis"

_tenant_token: str | None = None

async def _get_tenant_token() -> str:
    """获取 tenant_access_token"""
    global _tenant_token
    if _tenant_token:
        return _tenant_token

    async with httpx.AsyncClient() as client:
        resp = await client.post(
            f"{FEISHU_BASE}/auth/v3/tenant_access_token/internal",
            json={
                "app_id": settings.feishu_app_id,
                "app_secret": settings.feishu_app_secret,
            },
        )
        data = resp.json()
        _tenant_token = data["tenant_access_token"]
        return _tenant_token

async def send_text(chat_id: str, text: str) -> None:
    """发送纯文本消息"""
    token = await _get_tenant_token()
    async with httpx.AsyncClient() as client:
        await client.post(
            f"{FEISHU_BASE}/im/v1/messages",
            params={"receive_id_type": "chat_id"},
            headers={"Authorization": f"Bearer {token}"},
            json={
                "receive_id": chat_id,
                "msg_type": "text",
                "content": f'{{"text": "{text}"}}',
            },
        )

async def send_card(chat_id: str, card: dict) -> None:
    """发送交互卡片"""
    token = await _get_tenant_token()
    import json
    async with httpx.AsyncClient() as client:
        resp = await client.post(
            f"{FEISHU_BASE}/im/v1/messages",
            params={"receive_id_type": "chat_id"},
            headers={"Authorization": f"Bearer {token}"},
            json={
                "receive_id": chat_id,
                "msg_type": "interactive",
                "content": json.dumps(card, ensure_ascii=False),
            },
        )
        if resp.status_code != 200:
            logger.error(f"发送卡片失败: {resp.text}")

def build_assessment_card(
    student_name: str,
    grade: int,
    topic: str,
    scores: dict[str, int],
    weighted_score: float,
    reasoning: str,
    highlights: str,
    suggestions: str,
    overall_comment: str,
    needs_review: bool,
    assessment_id: int,
    source_type: str = "text",
    retry_count: int = 0,
) -> dict:
    """构建评估结果飞书卡片"""

    # 星级显示
    def stars(n: int) -> str:
        return "★" * n + "☆" * (5 - n)

    # 维度行
    dim_lines = []
    for dim, score in scores.items():
        # 1-2年级不显示逻辑和多角度
        if grade <= 2 and dim in ("逻辑连贯性", "多角度思考"):
            continue
        dim_lines.append(f"{dim}  {stars(score)}  {score}/5")

    dims_text = "n".join(dim_lines)

    # 来源标签
    source_label = {"handwriting": "📸 手写作文", "voice": "🎤 语音", "text": "✍️ 文字"}.get(source_type, "✍️ 文字")

    # 标题
    title = f"📊 思辨评估 - {student_name}"
    if retry_count > 0:
        title += f"（第{retry_count + 1}次评估）"

    # 按钮
    actions = []
    if not needs_review:
        actions = [
            {"tag": "button", "text": {"tag": "plain_text", "content": "✅ 确认"}, "type": "primary",
             "value": {"action": "confirm", "assessment_id": str(assessment_id)}},
            {"tag": "button", "text": {"tag": "plain_text", "content": "✏️ 修改分数"}, "type": "default",
             "value": {"action": "modify", "assessment_id": str(assessment_id)}},
        ]
        if retry_count < 2:
            actions.append(
                {"tag": "button", "text": {"tag": "plain_text", "content": "🔄 打回重评"}, "type": "default",
                 "value": {"action": "retry", "assessment_id": str(assessment_id)}}
            )
        actions.append(
            {"tag": "button", "text": {"tag": "plain_text", "content": "👎 完全不对"}, "type": "danger",
             "value": {"action": "disagree", "assessment_id": str(assessment_id)}}
        )
    else:
        actions = [
            {"tag": "button", "text": {"tag": "plain_text", "content": "👎 标记待复核"}, "type": "danger",
             "value": {"action": "disagree", "assessment_id": str(assessment_id)}}
        ]

    card = {
        "config": {"wide_screen_mode": True},
        "header": {
            "title": {"tag": "plain_text", "content": title},
            "template": "red" if needs_review else "blue",
        },
        "elements": [
            {
                "tag": "div",
                "text": {
                    "tag": "lark_md",
                    "content": f"主题：{topic} | 年级：{grade} | 来源：{source_label}",
                },
            },
            {"tag": "hr"},
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": dims_text},
            },
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"n综合评分：{weighted_score} / 5.0"},
            },
            {"tag": "hr"},
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"📝 评分理由：{reasoning}"},
            },
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"⭐ 亮点：{highlights}"},
            },
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"📌 建议：{suggestions}"},
            },
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"💬 总评：{overall_comment}"},
            },
            {"tag": "hr"},
            {"tag": "action", "actions": actions},
        ],
    }

    return card

app/api/webhook.py（更新：串联评估）

在 webhook.py 的 text 消息处理部分，替换 TODO：

from app.services.llm_service import call_llm_assessment
from app.services.feishu_service import send_card, send_text, build_assessment_card
from app.domain.schemas import ParsedCommand

处理评估请求
if parsed.action == "assess":
    # 检查必填字段
    missing = []
    if not parsed.student_name:
        missing.append("学生姓名")
    if not parsed.grade:
        missing.append("年级")
    if not parsed.topic:
        missing.append("主题")
    if not parsed.text:
        missing.append("学生发言内容")

    if missing:
        await send_text(chat_id, f"请补充以下信息：{', '.join(missing)}nn格式：评估 学生:小明 年级:3 主题:动物穿衣服 发言:我觉得...")
        return {"code": 0}

    # 发送"正在评估"提示
    await send_text(chat_id, f"📝 正在评估 {parsed.student_name} 的表现...")

    # 调用评估
    result = await call_llm_assessment(
        text=parsed.text,
        grade=parsed.grade,
        topic=parsed.topic,
        student_id=parsed.student_name,  # MVP 阶段用姓名作为 ID
        class_id=chat_id,
        source_type="text",
    )

    # 构建卡片并发送
    card = build_assessment_card(
        student_name=parsed.student_name,
        grade=parsed.grade,
        topic=parsed.topic,
        scores=result.scores,
        weighted_score=result.weighted_score,
        reasoning=result.reasoning,
        highlights=result.highlights,
        suggestions=result.suggestions,
        overall_comment=result.overall_comment,
        needs_review=result.needs_review,
        assessment_id=0,  # MVP 阶段暂用 0，Level 6 持久化后改为真实 ID
        source_type="text",
    )
    await send_card(chat_id, card)

elif parsed.action == "help":
    help_text = """📖 EduSocratic 使用说明

发送以下格式触发评估：
@EduSocratic 评估 学生:小明 年级:3 主题:动物穿衣服 发言:我觉得...

其他指令：
查询 小明 → 查看最近评估
生成周报 → 生成本周报告
帮助 → 显示本说明"""
    await send_text(chat_id, help_text)

return {"code": 0}

验收标准

tests/test_feishu_card.py

def test_build_assessment_card_structure():
    """卡片结构正确"""
    from app.services.feishu_service import build_assessment_card
    card = build_assessment_card(
        student_name="小明", grade=3, topic="动物穿衣服",
        scores={"观点清晰度": 4, "逻辑连贯性": 3, "证据支撑": 4,
                "多角度思考": 3, "回应与质疑": 3, "表达完整性": 4},
        weighted_score=3.6, reasoning="test", highlights="test",
        suggestions="test", overall_comment="test",
        needs_review=False, assessment_id=1,
    )
    assert card["header"]["title"]["content"] == "📊 思辨评估 - 小明"
    assert len(card["elements"]) > 5
    # 检查按钮存在
    actions = card["elements"][-1]
    assert actions["tag"] == "action"
    assert len(actions["actions"]) == 4  # 确认、修改、打回、不对

def test_build_assessment_card_grade_1_2_hides_dims():
    """1-2年级不显示逻辑和多角度"""
    from app.services.feishu_service import build_assessment_card
    card = build_assessment_card(
        student_name="小红", grade=1, topic="测试",
        scores={"观点清晰度": 4, "逻辑连贯性": 3, "证据支撑": 4,
                "多角度思考": 3, "回应与质疑": 3, "表达完整性": 4},
        weighted_score=3.8, reasoning="t", highlights="t",
        suggestions="t", overall_comment="t",
        needs_review=False, assessment_id=1,
    )
    # 卡片文本中不应包含"逻辑连贯性"和"多角度思考"
    import json
    card_str = json.dumps(card, ensure_ascii=False)
    assert "逻辑连贯性" not in card_str
    assert "多角度思考" not in card_str

完成标志

[ ] 收到评估请求后能调用 LLM 并返回卡片
[ ] 卡片结构正确（标题、维度、按钮）
[ ] 1-2年级卡片不显示逻辑和多角度
[ ] 缺少必填字段时能追问
[ ] uv run pytest tests/test_feishu_card.py 通过

Level 5：卡片按钮回调 + 审核迭代

目标

实现老师点击卡片按钮后的处理：确认、打回重评、不准确。

前置依赖

Level 4 完成。

要实现的文件

app/api/callback.py

"""飞书卡片按钮回调"""

import logging
from fastapi import APIRouter, Request
from app.services.feishu_service import send_text, send_card, build_assessment_card
from app.services.llm_service import call_llm_assessment

logger = logging.getLogger(name)
router = APIRouter()

MVP 阶段：内存存储评估状态（Level 6 改为数据库）
_assessment_store: dict[int, dict] = {}
_next_id: int = 1

def store_assessment(data: dict) -> int:
    """存储评估结果，返回 ID"""
    global _next_id
    aid = _next_id
    _next_id += 1
    _assessment_store[aid] = data
    return aid

@router.post("/callback")
async def card_callback(request: Request):
    """处理卡片按钮点击"""
    body = await request.json()

    # 飞书卡片回调格式
    action = body.get("action", {})
    action_value = action.get("value", {})
    action_type = action_value.get("action", "")
    assessment_id = int(action_value.get("assessment_id", 0))
    operator_id = body.get("operator", {}).get("open_id", "")
    open_chat_id = body.get("open_chat_id", "")

    if assessment_id not in _assessment_store:
        return {"toast": {"type": "error", "content": "评估记录不存在"}}

    record = _assessment_store[assessment_id]

    match action_type:
        case "confirm":
            record["status"] = "confirmed"
            # TODO: 触发家长通知（Phase 2）
            return {"toast": {"type": "success", "content": "✅ 已确认"}}

        case "retry":
            if record.get("retry_count", 0) >= 2:
                return {"toast": {"type": "error", "content": "已打回2次，请确认或修改分数"}}

            # 提示老师输入上下文
            await send_text(
                open_chat_id,
                "🔄 请补充课堂上下文，帮助我更准确地评估：n"
                "例如：'小明是在反驳小红的观点'n"
                "例如：'这是小组讨论的总结发言'nn"
                "直接回复即可（2分钟内有效）"
            )
            record["status"] = "retrying"
            record["waiting_context"] = True
            return {"toast": {"type": "info", "content": "请输入补充上下文"}}

        case "disagree":
            record["status"] = "disputed"
            return {"toast": {"type": "warning", "content": "👎 已标记为不准确，将人工复核"}}

        case "modify":
            await send_text(
                open_chat_id,
                "✏️ 请发送修改后的分数：n"
                "格式：观点4 逻辑3 证据4 多角度3 回应3 表达4"
            )
            record["status"] = "modifying"
            record["waiting_modify"] = True
            return {"toast": {"type": "info", "content": "请输入修改后分数"}}

    return {"toast": {"type": "info", "content": "未知操作"}}

app/api/webhook.py（更新：处理打回上下文回复）

在 webhook.py 中，处理 text 消息时增加：

检查是否在等待打回上下文
from app.api.callback import _assessment_store

查找是否有 waiting_context 的记录（属于当前 chat）
waiting_record = None
for aid, rec in _assessment_store.items():
    if rec.get("waiting_context") and rec.get("class_id") == chat_id:
        waiting_record = (aid, rec)
        break

if waiting_record:
    aid, rec = waiting_record
    rec["waiting_context"] = False
    rec["retry_count"] = rec.get("retry_count", 0) + 1
    teacher_context = text_content  # 老师输入的上下文

    await send_text(chat_id, "收到！正在结合上下文重新评估...")

    # 重新评估
    result = await call_llm_assessment(
        text=rec["text"],
        grade=rec["grade"],
        topic=rec["topic"],
        student_id=rec["student_id"],
        class_id=chat_id,
        source_type=rec.get("source_type", "text"),
        teacher_context=teacher_context,
        retry_count=rec["retry_count"],
    )

    # 更新记录
    rec["scores"] = result.scores
    rec["weighted_score"] = result.weighted_score
    rec["status"] = "pending"
    rec["teacher_context"] = teacher_context

    # 发送新卡片
    card = build_assessment_card(
        student_name=rec["student_name"],
        grade=rec["grade"],
        topic=rec["topic"],
        scores=result.scores,
        weighted_score=result.weighted_score,
        reasoning=result.reasoning,
        highlights=result.highlights,
        suggestions=result.suggestions,
        overall_comment=result.overall_comment,
        needs_review=result.needs_review,
        assessment_id=aid,
        source_type=rec.get("source_type", "text"),
        retry_count=rec["retry_count"],
    )
    await send_card(chat_id, card)
    return {"code": 0}

app/main.py（更新）

from fastapi import FastAPI
from app.api.webhook import router as webhook_router
from app.api.callback import router as callback_router

app = FastAPI(title="EduSocratic", version="0.1.0")
app.include_router(webhook_router)
app.include_router(callback_router)

@app.get("/health")
async def health():
    return {"status": "ok", "version": "0.1.0"}

验收标准

tests/test_callback.py

import pytest
from httpx import AsyncClient, ASGITransport
from app.main import app

@pytest.mark.asyncio
async def test_confirm_callback():
    """确认按钮回调"""
    from app.api.callback import store_assessment
    aid = store_assessment({"status": "pending", "student_name": "小明"})

    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.post("/callback", json={
            "action": {"value": {"action": "confirm", "assessment_id": str(aid)}},
            "operator": {"open_id": "ou_teacher"},
            "open_chat_id": "oc_test",
        })
        assert resp.json()["toast"]["type"] == "success"

@pytest.mark.asyncio
async def test_retry_callback():
    """打回按钮回调"""
    from app.api.callback import store_assessment, _assessment_store
    aid = store_assessment({"status": "pending", "retry_count": 0, "class_id": "oc_test"})

    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.post("/callback", json={
            "action": {"value": {"action": "retry", "assessment_id": str(aid)}},
            "operator": {"open_id": "ou_teacher"},
            "open_chat_id": "oc_test",
        })
        assert resp.json()["toast"]["type"] == "info"
        assert _assessment_store[aid]["status"] == "retrying"

@pytest.mark.asyncio
async def test_retry_limit():
    """打回超过2次被拒绝"""
    from app.api.callback import store_assessment
    aid = store_assessment({"status": "pending", "retry_count": 2})

    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.post("/callback", json={
            "action": {"value": {"action": "retry", "assessment_id": str(aid)}},
            "operator": {"open_id": "ou_teacher"},
            "open_chat_id": "oc_test",
        })
        assert resp.json()["toast"]["type"] == "error"

完成标志

[ ] 点击"确认"→ 状态变为 confirmed
[ ] 点击"打回"→ 提示输入上下文 → 重新评估 → 新卡片
[ ] 打回超过 2 次 → 拒绝
[ ] 点击"不准确"→ 状态变为 disputed
[ ] uv run pytest tests/test_callback.py 通过

Level 6：持久化（PostgreSQL）

目标

将内存存储替换为 PostgreSQL，评估记录、教师反馈持久化。

前置依赖

Level 5 完成。

要实现的文件

app/models/tables.py

"""SQLAlchemy 模型"""

from datetime import datetime
from sqlalchemy import Column, Integer, String, Text, Float, Boolean, DateTime, JSON
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass

class Assessment(Base):
    tablename = "assessments"

    id = Column(Integer, primary_key=True, autoincrement=True)
    student_id = Column(String(64), nullable=False, index=True)
    class_id = Column(String(64), nullable=False, index=True)
    grade = Column(Integer, nullable=False)
    topic = Column(String(256), nullable=False)
    source_type = Column(String(16), default="text")
    response_text = Column(Text, nullable=False)
    scores = Column(JSON, nullable=False)
    weighted_score = Column(Float, nullable=False)
    reasoning = Column(Text)
    highlights = Column(Text)
    suggestions = Column(Text)
    overall_comment = Column(Text)
    status = Column(String(16), default="pending", index=True)
    retry_count = Column(Integer, default=0)
    teacher_context = Column(Text)
    final_scores = Column(JSON)
    teacher_comment = Column(Text)
    needs_review = Column(Boolean, default=False)
    llm_model = Column(String(64))
    prompt_version = Column(String(16), default="v1.0")
    latency_ms = Column(Integer)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

class TeacherFeedbackRecord(Base):
    tablename = "teacher_feedbacks"

    id = Column(Integer, primary_key=True, autoincrement=True)
    assessment_id = Column(Integer, nullable=False, index=True)
    teacher_id = Column(String(64), nullable=False)
    action = Column(String(16), nullable=False)
    modified_scores = Column(JSON)
    teacher_context = Column(Text)
    comment = Column(Text)
    created_at = Column(DateTime, default=datetime.utcnow)

app/services/db.py

"""数据库连接与会话管理"""

from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from app.config import settings

engine = create_async_engine(settings.database_url, echo=False)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

async def get_session() -> AsyncSession:
    async with async_session() as session:
        yield session

验收标准

创建数据库
createdb edusocratic

运行迁移（或直接用 metadata.create_all）
uv run python -c "
import asyncio
from app.models.tables import Base
from app.services.db import engine

async def main():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    print('Tables created')

asyncio.run(main())
"

测试写入和读取
uv run pytest tests/test_db.py

完成标志

[ ] 数据库表创建成功
[ ] 评估结果能写入 assessments 表
[ ] 教师反馈能写入 teacher_feedbacks 表
[ ] callback.py 中的内存存储替换为数据库操作

Level 7：缓存 + 熔断

目标

加入 Redis 缓存（相同输入不重复调 LLM）和熔断（日配额限制）。

前置依赖

Level 6 完成。

要实现的文件

app/services/cache_service.py

"""Redis 缓存与熔断"""

import hashlib
import json
import redis.asyncio as redis
from app.config import settings

_redis: redis.Redis | None = None

async def get_redis() -> redis.Redis:
    global _redis
    if _redis is None:
        _redis = redis.from_url(settings.redis_url, decode_responses=True)
    return _redis

def _cache_key(grade: int, topic: str, text: str) -> str:
    """生成缓存 key"""
    raw = f"{grade}:{topic}:{text}"
    return f"assessment:{hashlib.sha256(raw.encode()).hexdigest()[:16]}"

async def get_cached(grade: int, topic: str, text: str) -> dict | None:
    """查缓存"""
    try:
        r = await get_redis()
        key = _cache_key(grade, topic, text)
        data = await r.get(key)
        if data:
            return json.loads(data)
    except Exception:
        pass  # Redis 挂了不影响主流程
    return None

async def set_cached(grade: int, topic: str, text: str, result: dict) -> None:
    """写缓存"""
    try:
        r = await get_redis()
        key = _cache_key(grade, topic, text)
        await r.setex(key, settings.cache_ttl_seconds, json.dumps(result, ensure_ascii=False))
    except Exception:
        pass

async def check_daily_limit(student_id: str) -> tuple[bool, int]:
    """检查日配额。返回 (是否允许, 已用次数)"""
    try:
        r = await get_redis()
        from datetime import date
        key = f"daily_limit:{student_id}:{date.today().isoformat()}"
        count = await r.incr(key)
        if count == 1:
            await r.expire(key, 86400)  # 24h 过期
        if count > settings.daily_limit_per_student:
            return False, count
        return True, count
    except Exception:
        return True, 0  # Redis 挂了不限制

验收标准

tests/test_cache.py

@pytest.mark.asyncio
async def test_cache_hit():
    """相同输入命中缓存"""
    from app.services.cache_service import set_cached, get_cached
    await set_cached(3, "test", "hello", {"score": 4})
    result = await get_cached(3, "test", "hello")
    assert result == {"score": 4}

@pytest.mark.asyncio
async def test_cache_miss():
    """不同输入不命中"""
    from app.services.cache_service import get_cached
    result = await get_cached(3, "test", "different")
    assert result is None

@pytest.mark.asyncio
async def test_daily_limit():
    """日配额限制"""
    from app.services.cache_service import check_daily_limit
    # 模拟 20 次调用
    for i in range(20):
        allowed, count = await check_daily_limit("test_student_limit")
        assert allowed is True
    # 第 21 次
    allowed, count = await check_daily_limit("test_student_limit")
    assert allowed is False

完成标志

[ ] 相同输入第二次调用直接返回缓存
[ ] 超过日配额返回友好提示
[ ] Redis 挂了不影响主流程（降级为无缓存）

Level 总览
Level   内容   核心产出   预计时间
0   项目骨架   可运行的 FastAPI 空项目   10 min

1   领域模型   Pydantic schemas + 年级权重   20 min

2   Prompt + LLM   评估管道（调用→校验→兜底）   40 min

3   飞书 Webhook   消息接收 + 指令解析   30 min

4   飞书卡片   评估结果卡片 + 串联   30 min

5   审核迭代   确认/打回/不准确   40 min

6   持久化   PostgreSQL 存储   30 min

7   缓存熔断   Redis 缓存 + 配额   20 min

MVP 完成后的完整链路：

老师 @机器人 输入文字
  → Webhook 接收 + 解析
    → 检查缓存/配额
      → 调用 LLM 评估
        → 三段式校验 + 兜底
          → 构建飞书卡片
            → 发送到群
              → 老师点按钮
                → 确认 / 打回重评 / 不准确
                  → 持久化到 DB

给 Claude 的使用说明

每次对话只做一个 Level。

对话格式：
"请实现 Level N"

Claude 应该：
列出要创建/修改的文件
给出完整代码（不是片段）
给出测试代码
给出验收命令

完成一个 Level 后，运行验收命令确认通过，再进入下一个。